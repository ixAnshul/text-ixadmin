name: CICD

on: 
  push:
    branches: [master]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout source
        uses: actions/checkout@v3
      - name: Setup java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Build Project
        run: mvn clean install -DskipTests
      - name: Login to docker hub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
      - name: build docker image
        run: docker build -t test/text-ixadmin .
      - name: publish image to docker hub
        run: docker push test/text-ixadmin:latest

  deploy:
    needs: build
    runs-on: [aws-ec2]
    steps:
      - name: Pull Image from docker hub
        run: docker pull in test/text-ixadmin:latest
      - name: Delete old container
        run: docker rm -f test/text-ixadmin
      - name: Run docker container
        run: docker run -d -p 8082:8082 --name springboot-example-container test/text-ixadmin


